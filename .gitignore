# dependencies
node_modules/

# secrets / local environment (NEVER commit) — keep .env.example
.env
.env.*
!.env.example
server/.env

# admin allow-list contains a real email — keep it private (use the .example)
server/sql/admin-users.sql

# logs
*.log
npm-debug.log*

# OS / editor / tooling cruft
.DS_Store
Thumbs.db
.vscode/
.claude/

# build / preview artifacts
home-preview.png
prev-*.png
preview*.png
p-*.png
admin-test.html
client/admin-test.html
*.tmp
